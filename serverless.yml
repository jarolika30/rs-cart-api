service: cart-service
plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs18.x
  stage: dev
  region: us-east-1

functions:
  main:
    handler: dist/main.handler
    events:
      - http:
          method: ANY
          path: /
    environment:
      DB_HOST: rds-cloudx-staurova.czk7ppdubmsk.us-east-1.rds.amazonaws.com
      POSTGRES_PORT: 5432
      POSTGRES_USER: jarolika30
      POSTGRES_PASSWORD: password1234
      POSTGRES_DB: rds_cloudx_db
